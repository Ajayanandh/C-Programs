#include<stdio.h>
#include<conio.h>
int main(){
int n,sum=0,i ;
printf("Enter the number :");
scanf("%d",&n);
print: sum=sum+i;i++;
if(i<=n) goto print;
printf ("The sum of N numbers is %d ",sum);

}
